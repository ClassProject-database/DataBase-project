services:
  - type: web
    name: movie-rental-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Website.Backend.main:app
    envVars:
      - key: FLASK_ENV
        value: production
